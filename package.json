{ 
	"scripts": {
    	"dev": "cd app/ && next dev",
    	"build": "cd app/ && next build",
    	"start": "cd app/ && next start",
    	"lint": "cd app/ && next lint"
  },
	"devDependencies": {
		"@tauri-apps/cli": "^1.4.0"
	}
}
